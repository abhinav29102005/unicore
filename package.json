{
  "name": "unicore-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "UniCore: Centralized Campus Operating Platform Mono-repo",
  "workspaces": [
    "docs/hub",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:docs": "npm run dev --prefix docs/hub",
    "build:docs": "npm run build --prefix docs/hub",
    "dev": "npm run dev --prefix docs/hub",
    "build": "npm run build --prefix docs/hub"
  }
}
